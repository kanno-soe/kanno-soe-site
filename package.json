{
  "name": "kanno-soe-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:context": "node scripts/build-context.mjs",
    "fetch:pricing": "node scripts/fetch-pricing.mjs",
    "check": "tsc --noEmit",
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260706.1",
    "katex": "^0.18.1",
    "markdown-it": "^15.0.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.24.0"
  }
}
